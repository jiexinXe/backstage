import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ItemPage = () => (
  <div style={{ padding: '50px', textAlign: 'center' }}>
    <Title level={2}>这是商品界面</Title>
  </div>
);

export default ItemPage;
