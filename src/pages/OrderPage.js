import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const OrderPage = () => (
  <div style={{ padding: '50px', textAlign: 'center' }}>
    <Title level={2}>这是订单界面</Title>
  </div>
);

export default OrderPage;
